## 🐳 Docker 開発メモ

### 起動（基本：バックグラウンド）

```bash
# next-go-monorepo（docker-compose.yml がある場所）で
docker compose up -d --build
```

```bash
docker compose logs -f
```

```bash
docker compose down
```
